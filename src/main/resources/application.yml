spring:
  datasource:
    url: jdbc:mysql://localhost:3306/deploy
    username: root
    password: afSo7q64kRvGJj

  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: create-drop